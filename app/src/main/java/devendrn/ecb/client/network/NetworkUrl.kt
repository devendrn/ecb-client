package devendrn.ecb.client.network

object NetworkUrl {
    const val DOMAIN = "tkmce.etlab.in"
    const val HOME = "https://$DOMAIN/"
    const val USER_LOGIN = "${HOME}user/login"
    const val USER_LOGOUT = "${HOME}user/logout"
}